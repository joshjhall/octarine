//! Token types for redaction and data classification
//!
//! Re-exports token types from identifiers for the public API.
//! The canonical location for `RedactionToken` is `identifiers::RedactionToken`.

// Re-export RedactionToken from identifiers (the public API location)
pub use crate::identifiers::RedactionToken;
